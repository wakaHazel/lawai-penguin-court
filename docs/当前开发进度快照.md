# 当前开发进度快照

> 更新时间：2026-04-08
> 项目：企鹅法庭·沉浸式庭审模拟与立法沙盘推演 AI 智能体系统

## 1. 当前已完成

### 1.1 约束与方案文档

- 已整理赛题约束、官方工具链白名单、交付物清单、主轴锁定守则
- 已形成项目技术方案 V1 和 MVP 实施计划
- 已补团队分工与模板材料
- 已补《元器受阻期间并行推进总表》
- 已补《明日开工任务单（2026-04-09）》
- 已补首批法律素材草案 4 份，可直接交法学同学审校

### 1.2 案件录入骨架

- 后端已定义 `CaseProfile`、`EvidenceItem`、`OpponentProfile`、`TimelineEvent`
- 已做 `domain -> case_type` 强校验
- 前端已补案件录入草稿结构、字段配置、选项配置、草稿转结构化对象函数

### 1.3 庭审状态机骨架

- 已新增 `TrialStage`
- 已新增 `SimulationSnapshot`
- 已实现从案件录入到模拟启动的最小链路
- 已实现从 `prepare` 推进到 `investigation` 的一步状态推进
- 已实现从 `prepare` 一直推进到 `report_ready` 的完整阶段链路
- 已修正终态 `report_ready` 不再继续虚增回合数

### 1.4 前端接线骨架

- 已新增前端 API 请求封装
- 已新增案件创建、案件详情读取、模拟启动、回合推进函数
- 已新增庭审阶段中文映射
- 已新增庭审页状态模型与推进请求构造函数
- 已新增庭审模拟页控制器
- 已新增庭审页 JSX 骨架与阶段进度、场景面板、行动栏、案件侧栏组件
- 已新增前端错误边界组件

### 1.5 元器落地文档

- 已新增《元器工作流详细设计（庭审模拟链路）》
- 已明确主工作流拆分、节点顺序、变量设计、得理 API 插件接入方式、后端与元器分工边界
- 已新增《元器实施边界与约束（企鹅法庭）》
- 已新增《得理检索工具配置清单（元器）》
- 已新增《元器最小回传样例需求（变量面板）》
- 已新增元器工作流交接包 `handoff/yuanqi_workflow_handoff_2026-04-08`
- 已新增机器可读工具规格 `data/yuanqi_tool_specs.json`

### 1.6 元器桥接代码

- 已新增 `schemas/yuanqi.py`
- 已新增 `services/yuanqi_bridge.py`
- 已实现庭审场景生成工作流调用载荷构造
- 已实现法律依据检索工作流调用载荷构造
- 已通过测试锁定变量名、空值处理、关键词提取逻辑

### 1.7 已开放接口

- `POST /api/cases`
- `GET /api/cases/{case_id}`
- `POST /api/cases/{case_id}/simulate/start`
- `POST /api/cases/{case_id}/simulate/turn`
- `GET /health`

## 2. 当前未做

- 未拿到可反推变量面板编码的元器最小真实导出样例
- 未完成腾讯元器工作流的真实导入闭环
- 未接入得理开放平台 API
- 未实现对方行为模拟
- 未实现胜诉率分析
- 未实现复盘报告生成
- 未实现前端页面联调
- 未实现数据库持久化

## 2.1 当前明确阻塞

- 元器导入包当前仍不能稳定恢复 `工作流级变量 / 启动工作流输入变量 / 回复节点变量引用`
- 在缺少真实最小导出样例前，继续盲猜 `变量.xlsx / 参数.xlsx / workflow.json` 编码，返工风险高
- 得理工具不会随着 zip 自动创建，必须在元器中手工配置

## 2.2 当前可并行推进

- 手工创建元器最小真实样例并导出
- 手工创建得理工具：`search_case / search_law / get_law_info`
- 法学素材库建设：Demo 案件事实包、案由-争点-证据-法条映射表
- 初赛材料骨架、AI 工具使用说明、Demo 演示脚本、答辩问答库

## 3. 当前验证结果

- `python -m pytest E:\lawai\tests\api`
- 结果：`9 passed`

## 6. 当前前端限制

- 当前 `apps/web` 下仍没有 `package.json`
- 当前 `apps/web` 下仍没有 `tsconfig.json`
- 因此前端代码目前完成了结构搭建，但还没有做真实 TypeScript 编译校验

- `python -m py_compile ...`
- 结果：通过

## 4. 下一步建议顺序

1. 先拿到 `元器最小真实导出样例`，锁定变量面板真实编码
2. 手工完成得理工具配置：`search_case / search_law / get_law_info`
3. 同步补齐 4 个 MVP 案由的法律素材库
4. 基于真实样例回修元器正式工作流导入包
5. 做对方行为模拟、胜诉率分析、复盘报告
6. 再做前端完整页面联调与 demo 包装

## 5. 当前主轴

项目仍严格围绕这一条主轴推进：

`案件录入 -> 庭审模拟 -> 对方行为模拟 -> 胜诉率分析 -> 复盘报告`

不扩成通用多 agent 平台，不提前展开立法沙盘深水区。

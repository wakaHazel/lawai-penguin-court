# 元器实施边界与约束（企鹅法庭）

这份交接说明只强调一个结论：

- `导入包` 负责 `W00-W04` 工作流骨架
- `search_case / search_law / get_law_info` 需要在元器里单独手工配置
- `工作流级变量 / 启动工作流输入变量` 导入后必须人工核对

## 当前包里已经有的

- 5 个业务工作流 json
- 开始节点参数默认值
- `answer_text` 回复链
- 结束节点输出绑定

## 当前包里没有自动解决的

- 得理工具配置
- `appid / secret` Header 绑定
- 元器 UI 面板上的变量自动补全保证

## 先后顺序

1. 先建得理 3 个工具
2. 再导入 `W00-W04`
3. 再逐个检查开始节点参数、结束节点输出、回复节点引用
4. 优先联调 `W02`
5. 最后再联调 `W00`

## 必须记住

如果把“工具配置”和“工作流导入包”混成一件事，后面一定返工。


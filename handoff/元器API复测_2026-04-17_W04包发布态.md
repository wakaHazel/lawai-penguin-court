# 元器 API 复测（2026-04-17，W04 包发布态）

复测时间：2026-04-17  
复测对象：已发布助手 `appid=2044586395271431232`  
测试方式：`scripts/yuanqi_retest_matrix.py`

## 复测命令

```powershell
python .\scripts\yuanqi_retest_matrix.py --assistant-id 2044586395271431232 --api-key DubO0MR023pIRtzxI0Rq366C9LElVKGW
```

## 结果

### 1. 只发消息，不带 `current_stage`

- HTTP `200`
- 返回 fallback 文案：

`当前阶段参数不在预设范围内，无法匹配工作流分支。请检查 current_stage 是否为 prepare / investigation / evidence / debate / report_ready。`

这说明：

- 发布态助手能收到请求
- `W00` 的分支判断还在正常工作
- fallback 分支本身没坏

### 2. 一旦传入有效 `current_stage`

无论是：

- `prepare`
- `investigation`
- `evidence`
- `debate`
- `report_ready`

都统一返回：

- HTTP `500`
- `runtime_error`
- `code = 10003`

### 3. 带更多变量也一样

以下三种请求都仍然报 `500 / 10003`：

- `message + current_stage`
- `message + case_id + current_stage`
- `message + full W00 variables`

这说明：

- 问题不是“少传了某个业务字段”
- 问题仍然在工作流发布态内部执行链路

## 结论

截至 2026-04-17 这次复测：

- `round_number` 改成 `STRING` 并没有让已发布助手恢复正常
- 当前已发布版本仍然是：
  - fallback 可用
  - 任何有效阶段一进入真实分支就报 `500 / 10003`

因此现在不能说“发布态已经修好”。

## 更可能的剩余问题

结合 zip 结构复核，当前最可疑的剩余点还是：

1. `W00.大模型2` 没有输出 schema，但后面节点在引用它的 `Output.Content`
2. `W00.大模型3` 没有输出 schema，但后面节点在引用它的 `Output.Content`
3. `W04.大模型1` 没有输出 schema
4. `W02` 插件链路在发布态是否完全可用，仍需单独确认

## 一句话判断

你朋友在编辑器里看到“节点有输出”可能是真的，但**当前已发布助手对外调用仍然没跑通**。


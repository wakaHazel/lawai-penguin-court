# 元器工作流复核（W04 包）

复核时间：2026-04-17  
复核对象：`C:\Users\温岐\Desktop\export-W04_胜诉率分析与复盘等.zip`  
解压目录：`tmp/yuanqi_export_W04_2026-04-17`

## 本次确认已修复

### 1. `round_number` 已统一为 `STRING`

这次新包里已经不是 `INT` 了。

已确认位置：

- `W01_庭审场景生成.start.round_number = STRING`
- `W00 -> W01_庭审场景生成1.RefInputs.round_number = STRING`
- `W00 -> W01_庭审场景生成11.RefInputs.round_number = STRING`
- `W00` 自身的自定义变量 `round_number = STRING`

这说明你说的 “strings 已改完” 这一项，**在新包里已经落地**。

---

## 本次确认变量映射没有接错

这版 `W00` 不再从开始节点 `Inputs` 读参数，而是改成了 `CUSTOM_VARIABLE`。

通过 `变量.xlsx` 反查后，`CustomVarID -> 变量名` 对应关系是正常的：

- `47f22771... -> current_stage`
- `06dff05a... -> v_case_summary`
- `1f33cbf1... -> round_number`
- `3c51606c... -> v_historical_dialogs`
- `5deba3cc... -> case_id`
- `abf17397... -> v_case_type`
- `9bd258db... -> v_focus_issues`
- `150d957f... -> v_fact_keywords`
- `44f1fab3... -> v_opponent_role`
- `02d43d74... -> v_opponent_arguments`
- `fb7c58b5... -> selected_action`

所以这次不是“ID 绑错变量”的问题。

---

## 变量名不统一，但当前已做映射

这一类暂时不是直接阻塞项。

### `W00 -> W02`

- `W00.case_id -> W02.case_id`
- `W00.v_case_type -> W02.case_type`
- `W00.v_focus_issues -> W02.focus_issues_json`
- `W00.v_fact_keywords -> W02.fact_keywords_json`

### `W00 -> W03`

- `W00.v_opponent_role -> W03.v_opponent_role`
- `W00.current_stage -> W03.v_current_stage`
- `W00.selected_action -> W03.v_selected_action`
- `W00.v_opponent_arguments -> W03.v_likely_arguments`
- `W00.v_focus_issues -> W03.v_focus_issues`

### `W00 -> W04`

- `W00.v_case_summary -> W04.v_case_profile`
- `W02.Output.legal_payload -> W04.v_legal_support_summary`
- `W00.v_historical_dialogs -> W04.v_simulation_timeline`
- `W03.Output.opponent_payload -> W04.v_opponent_behavior`

判断：

- 这几组命名不统一
- 但当前映射是明确存在的
- **不会因为“名字不同”本身直接报错**

---

## 本次仍然残留的阻塞问题

### 1. `W00.大模型2` 仍然没有输出 schema

涉及节点：

- `W00_企鹅法庭主控编排 -> 大模型2`

当前状态：

- `Outputs = []`

但后续还在引用：

- `回复11.B_output -> 大模型2.Output.Content`
- `代码1.b_text -> 大模型2.Output.Content`

这仍然是结构性风险。

---

### 2. `W00.大模型3` 仍然没有输出 schema

涉及节点：

- `W00_企鹅法庭主控编排 -> 大模型3`

当前状态：

- `Outputs = []`

但后续还在引用：

- `回复12.C_output -> 大模型3.Output.Content`
- `代码1.c_text -> 大模型3.Output.Content`

这也是结构性风险。

---

### 3. `W04.大模型1` 仍然没有输出 schema

涉及节点：

- `W04_胜诉率分析与复盘 -> 大模型1`

当前状态：

- `Outputs = []`

虽然这版审计脚本没有把它列成 `broken_output_refs`，但节点本身依然没声明 `Output.Content`。  
这意味着：

- 如果 `结束` 节点或界面层仍默认依赖 `Output.Content`
- 那这里依然有运行时不稳定风险

建议仍按必修项处理。

---

### 4. `W02` 插件依赖仍然存在

涉及节点：

- `search_law1`
- `search_case1`

当前状态：

- 仍调用得理开放平台接口
- 仍依赖 `appid/secret`

所以：

- 这条链路必须单独在元器里实测
- 不能只看 zip 结构

---

## 当前阶段判断

### `prepare`

相比上一版，最大的硬伤 `round_number: INT` 已修。

所以 `prepare` 现在的主要风险已经下降很多。  
这条分支理论上比上一版更有希望跑通。

### `investigation / evidence / debate`

这三条分支仍会经过：

- `W01`
- `W02`
- `W03`
- `W00.大模型2`

所以即使 `round_number` 已修，`大模型2` 无输出 schema 这个问题仍足以让它们不稳定。

### `report_ready`

这条分支仍会经过：

- `W02`
- `W03`
- `W04.大模型1`
- `W00.大模型3`

因此它仍然是当前最危险的一条。

---

## 现在的逐项修复优先级

1. 给 `W00.大模型2` 补标准输出 schema
2. 给 `W00.大模型3` 补标准输出 schema
3. 给 `W04.大模型1` 补标准输出 schema
4. 单独调通 `W02` 两个插件
5. 再做五阶段全量调试

---

## 一句话结论

这版新包里：

- `round_number` 的 `STRING` 修复已经生效
- 各工作流之间的变量名虽然不统一，但映射没有接错
- **现在最主要的剩余问题，已经集中到 `W00/W04` 的 LLM 输出 schema 缺失**


# 元器 API 复测 2026-04-16 再次复测

## 复测对象
- assistant_id: `2044586395271431232`
- case_id: `case_live_verify_001`
- 接口: `https://open.hunyuan.tencent.com/openapi/v1/agent/chat/completions`
- 方式: 直接走 OpenAPI，不走发布页聊天框

## 真实结果
### 核心触发矩阵
1. `message=case_live_verify_001`，不传变量
   - HTTP `200`
   - 返回普通文本:
   - `当前阶段参数不在预设范围内... 请检查 current_stage 是否为 prepare / investigation / evidence / debate / report_ready`
2. `message=case_live_verify_001`，传 `current_stage=prepare`
   - HTTP `500`
   - `runtime_error / code 10003 / 工作流运行异常`
3. `message=case_live_verify_001`，传 `case_id + current_stage`
   - HTTP `500`
   - `runtime_error / code 10003 / 工作流运行异常`
4. `message=case_live_verify_001`，传完整 W00 变量
   - HTTP `500`
   - `runtime_error / code 10003 / 工作流运行异常`

### 阶段矩阵
以下 5 个合法阶段，全部一进真实分支就报错:
- `prepare` -> HTTP `500`, `10003`
- `investigation` -> HTTP `500`, `10003`
- `evidence` -> HTTP `500`, `10003`
- `debate` -> HTTP `500`, `10003`
- `report_ready` -> HTTP `500`, `10003`

## 当前可下的结论
1. 这个已发布助手不是“完全没绑定工作流”。
   - 证据: 不传 `current_stage` 时，会返回 W00 fallback 文案。
2. 这个已发布助手也不是“后端参数没传进去”。
   - 证据: 只要真的把 `current_stage` 传进去，就会从 fallback 分支切到真实执行路径，然后直接 `10003`。
3. 现在的阻塞点在元器工作流内部，而不是当前 Python 后端的 payload 组装。
   - 证据: 极简变量、半量变量、完整 W00 变量，结果都一样。

## 从导出包里看到的高风险点
### 1. W00 和 W01 的 `round_number` 都声明为 `INT`
- W00 开始节点: `round_number` 是 `INT`
- W01 开始节点: `round_number` 也是 `INT`
- 但 OpenAPI 的 `custom_variables` 实测只能安全传字符串。
- 这意味着 prepare 分支存在类型转换风险。

### 2. W02 导出里仍有可疑代码节点
- W02 的 `代码1` 是一个把字符串拆成数组的 Python 节点。
- 导出内容里这段脚本已经出现乱码级损坏，至少可读性和可靠性都不正常。
- trial/report 分支依赖 W02，所以这部分很可能仍然会炸。

### 3. W02 插件节点里仍然是硬编码 appid/secret
- 说明导出包本身还是敏感件。
- 也说明 W02 运行是否正常，仍然依赖插件凭据和插件配置状态。

### 4. W04 的入参契约本身也值得再核
- W04 需要的是摘要型输入:
  - `v_legal_support_summary`
  - `v_opponent_behavior`
- 但 W00 里传给 W04 的值更像上游完整 payload，而不是已经拆好的摘要文本。
- 即便前面子流能跑，W04 这里也存在契约错位风险。

## 给元器同学的最短排查顺序
1. 先在元器里单独调试 W01。
   - 输入:
   - `current_stage=prepare`
   - `v_case_summary=任意非空`
   - `round_number=1`
   - `v_historical_dialogs=空字符串`
   - 目标: 看 W01 单独跑不跑得通
2. 再单独调试 W02。
   - 先看 `代码1` 节点是否能独立通过
   - 再看两个得理插件节点是否能独立返回
3. 再单独调试 W03、W04。
   - 确认各自单跑都不报错
4. 最后再回到 W00 联调。
   - 只要 W01/W02/W03/W04 有任一子流单跑还报错，W00 一定继续 `10003`

## 本地复测脚本
- 已放入工作区:
- `scripts/yuanqi_retest_matrix.py`

运行方式:

```powershell
python .\scripts\yuanqi_retest_matrix.py --assistant-id 2044586395271431232 --api-key <你的key>
```

它会直接输出:
- 核心触发矩阵
- 合法阶段矩阵

## 结论
这次“再改过一次”之后，真实状态不是“修好了但后端没跟上”，而是:
- fallback 分支正常
- 真实工作流分支仍然全部报 `10003`
- 需要继续修元器内部节点或子工作流，而不是继续怀疑当前后端传参

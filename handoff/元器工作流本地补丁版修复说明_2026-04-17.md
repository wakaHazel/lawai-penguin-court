# 元器工作流本地补丁版修复说明（2026-04-17）

## 输出位置

- 目录：`E:\lawai\exports\yuanqi_local_patch_pkg_2026-04-17`
- 压缩包：`E:\lawai\exports\yuanqi_local_patch_pkg_2026-04-17.zip`

## 本次修复范围

本次不是从零重建元器工作流，而是基于最新真实导出包：

- `E:\lawai\tmp\yuanqi_export_W02_2026-04-17`

对其中三条关键工作流做本地补丁式修复：

- `W00` 主控编排
- `W02` 法律支持检索
- `W04` 胜诉率分析与复盘

## 本次已修复内容

### W00 主控编排

- 已修复分支汇总代码，不再把 `s1 + s2 + s3 + s4` 和 `b1 + b2 + b3 + b4` 直接拼接
- 现在改为按顺序选择首个非空分支结果返回
- 已补齐 `大模型2` 和 `大模型3` 的 `Output.Thought / Output.Content` 输出 schema
- 已把中间审理链路 `branch_name = "trail"` 修正为 `branch_name = "trial"`

### W02 法律支持检索

- 已把 `代码1` 的输入标准化逻辑改为：
  - 先尝试 `json.loads`
  - 若不是合法 JSON 数组，再退回逗号切分
- 已将两个检索插件的 `pageSize` 从 `5` 收敛到 `3`

### W04 胜诉率分析与复盘

- 已补齐报告 LLM 的 `Output.Thought / Output.Content` 输出 schema

## 静态审计结果

已运行：

```powershell
python E:\lawai\scripts\yuanqi_workflow_zip_audit.py --dir E:\lawai\exports\yuanqi_local_patch_pkg_2026-04-17
```

本次修复目标对应的阻断项已不再出现：

- `W00` 不再报关键 LLM 缺失输出 schema
- `W02` 不再保留旧版标准化代码
- `W04` 不再报空输出 schema

当前审计仍会提示插件依赖，这是正常现象，因为导出包中本来就内嵌了插件头部配置。

## 当前残余风险

### 1. 插件密钥仍然存在于导出 JSON 中

这次没有改插件鉴权模型，只保留了当前导出包里的头部值。  
如果你们后面要交给别人导入或公开流转，需要再做一轮密钥替换。

### 2. 本次只做本地结构修复，不覆盖元器线上发布配置

这份包的定位是：

- 更稳的本地导入版
- 便于继续在元器里调试

它不等于“线上发布配置也一起修好”。

### 3. 本次没有大改 prompt 和业务链路

为了避免把你朋友已经调过的真实逻辑改坏，这次只修结构，不做大规模业务改写。

## 建议使用方式

### 给元器同学的建议顺序

1. 先导入：`E:\lawai\exports\yuanqi_local_patch_pkg_2026-04-17.zip`
2. 先只检查 `W00 / W02 / W04`
3. 确认三个工作流的节点结构与引用路径正常
4. 再做元器侧的真实输入联调
5. 如果仍有问题，优先排查：
   - 元器启动变量是否真的传入
   - 插件鉴权是否仍有效
   - 发布助手与工作流调试页是否使用了同一版本

## 关联脚本

- 构建脚本：`E:\lawai\scripts\build_yuanqi_local_patch_package.py`
- 审计脚本：`E:\lawai\scripts\yuanqi_workflow_zip_audit.py`

## 关联测试

已运行：

```powershell
pytest E:\lawai\tests\scripts\test_build_yuanqi_local_patch_package.py -q
```

结果：

```text
3 passed
```
